{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "dev": "npx tsx src/index.ts",
    "postinstall": "npx prisma generate"
  }
}
